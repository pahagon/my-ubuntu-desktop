---
  - name: "Install Vim"
    hosts: localhost
    connection: local
    become: true
    vars_files:
      - common_vars.yml
    tasks:
      - name: Update apt cache
        apt:
          update_cache: yes

      - name: Install vim
        package:
          name: vim
          state: latest
