---
  - name: "Install xsel"
    hosts: localhost
    connection: local
    become: true
    vars_files:
      - common_vars.yml
    tasks:
      - name: Update apt cache
        apt:
          update_cache: yes

      - name: Install xsel
        package:
          name: xsel
          state: latest
